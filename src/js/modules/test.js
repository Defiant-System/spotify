
let Test = {
	init(APP) {

		// setTimeout(() => APP.els.body.find(`span.ctrl-library`).trigger("click"), 700);
		// setTimeout(() => APP.els.body.find(`ul[data-click="select-playlist"] li:nth(2)`).trigger("click"), 1000);
		
	}
};
